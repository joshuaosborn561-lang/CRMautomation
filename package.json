{
  "name": "crm-autopilot",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "npm run start --workspace=fireflies-railway-webhook",
    "start:fireflies": "npm run start --workspace=fireflies-railway-webhook",
    "build": "npm run build --workspaces --if-present",
    "test:fireflies-rules": "npm run test:ci-rules --workspace=fireflies-railway-webhook"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
